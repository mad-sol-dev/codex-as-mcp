{
  "mcpServers": {
    "codex": {
      "type": "stdio",
      "command": "uv",
      "args": ["run", "python", "-m", "codex_as_mcp"],
      "cwd": "/home/martinm/programme/Projekte/codex-as-mcp",
      "env": {
        "MCP_SERVER_REQUEST_TIMEOUT": "300000",
        "MCP_REQUEST_TIMEOUT_RESET_ON_PROGRESS": "true",
        "MCP_REQUEST_MAX_TOTAL_TIMEOUT": "28800000"
      }
    }
  }
}
